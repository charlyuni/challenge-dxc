import React from 'react';

export const styles: { [key: string]: React.CSSProperties } = {
	boxContainer: {
		position: 'relative',
		top: '50px',
		bottom: '25px',
		display: 'flex',
		justifyContent: 'center',
	},
};
